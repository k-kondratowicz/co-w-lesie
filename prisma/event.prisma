model Event {
  id          String    @id @default(cuid())
  type        EventType
  lat         Float
  lng         Float
  description String?
  createdAt   DateTime  @default(now())
  endedAt     DateTime?
}

enum EventType {
  BLOOD
  FIRE
  HUNTING
  SHOTS
  VACCINATION
}
